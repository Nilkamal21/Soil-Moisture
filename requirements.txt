# Core packages for training and inference
tensorflow==2.15.0
tensorflow-cpu==2.15.0        # Optional: use if GPU support not needed
numpy==1.24.3
pandas==2.0.3

# Image processing
opencv-python==4.8.0.76
Pillow==10.2.0

# Model conversion & optimization
tensorflow-lite==0.0.0         # Ensure tflite_model_maker support
tflite-model-maker==0.4.2

# Audio I/O and multilingual support (offline-capable options)
vosk==0.3.45                  # Offline speech recognition
gtts==2.4.0                   # Google TTS (needs internet, but lightweight)
pyttsx3==2.90                 # Offline TTS for Windows, Mac, Linux

# Utility and scripting
scikit-learn==1.3.0

# (Optional for notebook-based development)
jupyter==1.0.0

# (Optional for YAML/JSON configs)
PyYAML==6.0

SpeechRecognition
gTTS
pyaudio